// Switch between Firebase and local auth by changing the import
// For Firebase (production):
export { AuthProvider, useAuth } from './AuthContextFirebase';

// For local demo (no Firebase setup required):
// export { AuthProvider, useAuth } from './AuthContext';
